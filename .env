# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

APP_ENV=prod
APP_SECRET=your_secret_key_here
APP_DEBUG=0

DATABASE_URL="mysql://root:password@127.0.0.1:3306/clinic_management?serverVersion=8.0&charset=utf8mb4"

MAILER_DSN=smtp://localhost:1025

REDIS_URL=redis://localhost:6379

CLINIC_NAME="Медицинская клиника"
CLINIC_ADDRESS="г. Москва, ул. Медицинская, д. 1"
CLINIC_PHONE="+7 (999) 123-45-67"
CLINIC_EMAIL="info@clinic.ru"

JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=your_passphrase_here

UPLOAD_MAX_SIZE=10M
ALLOWED_EXTENSIONS=jpg,jpeg,png,pdf,doc,docx
UPLOAD_DIR=%kernel.project_dir%/var/storage

CACHE_POOL=app.cache
CACHE_LIFETIME=3600

AUDIT_LOG_ENABLED=true
AUDIT_LOG_RETENTION_DAYS=90

FEATURE_EMAIL_NOTIFICATIONS=true
FEATURE_SMS_NOTIFICATIONS=false
FEATURE_ONLINE_PAYMENTS=false
FEATURE_TELEMEDICINE=false